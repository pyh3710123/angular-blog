import { StrucDirectiveDirective } from './struc-directive.directive';

describe('StrucDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StrucDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
