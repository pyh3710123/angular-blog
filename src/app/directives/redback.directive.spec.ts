import { RedbackDirective } from './redback.directive';

describe('RedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new RedbackDirective();
    expect(directive).toBeTruthy();
  });
});
